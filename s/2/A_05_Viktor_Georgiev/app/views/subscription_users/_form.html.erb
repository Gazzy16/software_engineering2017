<%= form_with(model: subscription_user, local: true) do |form| %>
  <% if subscription_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription_user.errors.count, "error") %> prohibited this subscription_user from being saved:</h2>

      <ul>
      <% subscription_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id %>
  </div>

  <div class="field">
    <%= form.label :userid %>
    <%= form.number_field :userid %>
  </div>

  <div class="field">
    <%= form.label :subscriptionid %>
    <%= form.number_field :subscriptionid %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.number_field :role %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
