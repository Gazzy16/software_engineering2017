<%= form_with(model: sub_user, local: true) do |form| %>
  <% if sub_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_user.errors.count, "error") %> prohibited this sub_user from being saved:</h2>

      <ul>
      <% sub_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :sub_user_user_id %>
  </div>

  <div class="field">
    <%= form.label :sub_id %>
    <%= form.number_field :sub_id, id: :sub_user_sub_id %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.number_field :role, id: :sub_user_role %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
