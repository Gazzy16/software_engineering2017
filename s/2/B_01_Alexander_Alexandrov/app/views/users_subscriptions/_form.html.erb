<%= form_with(model: users_subscription, local: true) do |form| %>
  <% if users_subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_subscription.errors.count, "error") %> prohibited this users_subscription from being saved:</h2>

      <ul>
      <% users_subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :subscription_id %>
    <%= form.number_field :subscription_id %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.text_field :role %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
