<%= form_with(model: specialty, local: true) do |form| %>
  <% if specialty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(specialty.errors.count, "error") %> prohibited this specialty from being saved:</h2>

      <ul>
      <% specialty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :specialty_name %>
  </div>

  <div class="field">
    <%= form.label :studnum %>
    <%= form.number_field :studnum, id: :specialty_studnum %>
  </div>

  <div class="field">
    <%= form.label :school_id %>
    <%= form.number_field :school_id, id: :specialty_school_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
