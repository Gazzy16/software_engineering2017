<%= form_with(model: school_specialty, local: true) do |form| %>
  <% if school_specialty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(school_specialty.errors.count, "error") %> prohibited this school_specialty from being saved:</h2>

      <ul>
      <% school_specialty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :specialty_id %>
    <%= form.number_field :specialty_id, id: :school_specialty_specialty_id %>
  </div>

  <div class="field">
    <%= form.label :school_id %>
    <%= form.number_field :school_id, id: :school_specialty_school_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
