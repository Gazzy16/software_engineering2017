<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :room_number %>
  </div>

  <div class="field">
    <%= form.label :area %>
    <%= form.number_field :area, id: :room_area %>
  </div>

  <div class="field">
    <%= form.label :hotel_id %>
    <%= form.text_field :hotel_id, id: :room_hotel_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
