<%= form_with(model: mschool, local: true) do |form| %>
  <% if mschool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mschool.errors.count, "error") %> prohibited this mschool from being saved:</h2>

      <ul>
      <% mschool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :mschool_name %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :mschool_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
