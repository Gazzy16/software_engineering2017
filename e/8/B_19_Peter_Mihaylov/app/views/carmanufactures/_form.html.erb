<%= form_with(model: carmanufacture, local: true) do |form| %>
  <% if carmanufacture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carmanufacture.errors.count, "error") %> prohibited this carmanufacture from being saved:</h2>

      <ul>
      <% carmanufacture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
