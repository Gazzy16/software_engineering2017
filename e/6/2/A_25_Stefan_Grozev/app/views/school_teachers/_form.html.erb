<%= form_with(model: school_teacher, local: true) do |form| %>
  <% if school_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(school_teacher.errors.count, "error") %> prohibited this school_teacher from being saved:</h2>

      <ul>
      <% school_teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :school_id %>
    <%= form.text_field :school_id, id: :school_teacher_school_id %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id, id: :school_teacher_teacher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
