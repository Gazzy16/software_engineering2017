<%= form_with(model: school, local: true) do |form| %>
  <% if school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(school.errors.count, "error") %> prohibited this school from being saved:</h2>

      <ul>
      <% school.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :school_day %>
    <%= form.text_field :school_day, id: :school_school_day %>
  </div>

  <div class="field">
    <%= form.label :teacherid %>
    <%= form.number_field :teacherid, id: :school_teacherid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
