<%= form_with(model: threadr, local: true) do |form| %>
  <% if threadr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(threadr.errors.count, "error") %> prohibited this threadr from being saved:</h2>

      <ul>
      <% threadr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id, id: :threadr_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :threadr_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
