<%= form_for([@thread_martin_m.process_martin_m, @thread_martin_m]) do |f| %>
  <% if @thread_martin_m.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thread_martin_m.errors.count, "error") %> prohibited this thread_martin_m from being saved:</h2>

      <ul>
      <% @thread_martin_m.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
