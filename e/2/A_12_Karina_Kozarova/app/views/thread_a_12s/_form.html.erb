<%= form_with(model: thread_a_12, local: true) do |form| %>
  <% if thread_a_12.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(thread_a_12.errors.count, "error") %> prohibited this thread_a_12 from being saved:</h2>

      <ul>
      <% thread_a_12.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_process %>
    <%= form.text_field :id_process, id: :thread_a_12_id_process %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
