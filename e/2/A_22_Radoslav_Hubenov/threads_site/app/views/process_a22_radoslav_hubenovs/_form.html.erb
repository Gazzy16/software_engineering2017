<%= form_with(model: process_a22_radoslav_hubenov, local: true) do |form| %>
  <% if process_a22_radoslav_hubenov.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(process_a22_radoslav_hubenov.errors.count, "error") %> prohibited this process_a22_radoslav_hubenov from being saved:</h2>

      <ul>
      <% process_a22_radoslav_hubenov.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :process_a22_radoslav_hubenov_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
