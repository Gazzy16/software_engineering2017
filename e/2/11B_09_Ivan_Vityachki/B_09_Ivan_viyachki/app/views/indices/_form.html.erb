<%= form_with(model: index, local: true) do |form| %>
  <% if index.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(index.errors.count, "error") %> prohibited this index from being saved:</h2>

      <ul>
      <% index.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ClassName %>
    <%= form.text_field :ClassName, id: :index_ClassName %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :index_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
