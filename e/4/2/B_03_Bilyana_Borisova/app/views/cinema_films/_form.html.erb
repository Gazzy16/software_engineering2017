<%= form_with(model: cinema_film, local: true) do |form| %>
  <% if cinema_film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cinema_film.errors.count, "error") %> prohibited this cinema_film from being saved:</h2>

      <ul>
      <% cinema_film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :cinema_film_location %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :cinema_film_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
