<%= form_with(model: cinemas_film, local: true) do |form| %>
  <% if cinemas_film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cinemas_film.errors.count, "error") %> prohibited this cinemas_film from being saved:</h2>

      <ul>
      <% cinemas_film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
