<%= form_with(model: cinema, local: true) do |form| %>
  <% if cinema.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cinema.errors.count, "error") %> prohibited this cinema from being saved:</h2>

      <ul>
      <% cinema.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :cinema_location %>
  </div>

  <div class="field">
    <%= form.label :id_sinema %>
    <%= form.number_field :id_sinema, id: :cinema_id_sinema %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
