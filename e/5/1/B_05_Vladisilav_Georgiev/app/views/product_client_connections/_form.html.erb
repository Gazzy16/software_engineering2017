<%= form_with(model: product_client_connection, local: true) do |form| %>
  <% if product_client_connection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_client_connection.errors.count, "error") %> prohibited this product_client_connection from being saved:</h2>

      <ul>
      <% product_client_connection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id, id: :product_client_connection_product_id %>
  </div>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.number_field :client_id, id: :product_client_connection_client_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
