<p id="notice"><%= notice %></p>

<h1>Client Products</h1>

<table>
  <thead>
    <tr>
      <th>Client</th>
      <th>Product</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @client_products.each do |client_product| %>
      <tr>
        <td><%= client_product.client.name %></td>
        <% client_product.client.client_products.each do |c| %>
          <td> <%= @client_products.find(c.client_id).product.name %> </td>
        <% end %>
        <td><%= link_to 'Show', client_product %></td>
        <td><%= link_to 'Edit', edit_client_product_path(client_product) %></td>
        <td><%= link_to 'Destroy', client_product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Client Product', new_client_product_path %>
