<%= form_with(model: tems_to_member, local: true) do |form| %>
  <% if tems_to_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tems_to_member.errors.count, "error") %> prohibited this tems_to_member from being saved:</h2>

      <ul>
      <% tems_to_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team_color %>
    <%= form.text_field :team_color, id: :tems_to_member_team_color %>
  </div>

  <div class="field">
    <%= form.label :member_name %>
    <%= form.text_field :member_name, id: :tems_to_member_member_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
