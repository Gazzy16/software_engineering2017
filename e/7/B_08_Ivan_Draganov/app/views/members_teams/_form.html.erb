<%= form_with(model: members_team, local: true) do |form| %>
  <% if members_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(members_team.errors.count, "error") %> prohibited this members_team from being saved:</h2>

      <ul>
      <% members_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :member_id %>
    <%= form.number_field :member_id, id: :members_team_member_id %>
  </div>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.number_field :team_id, id: :members_team_team_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
