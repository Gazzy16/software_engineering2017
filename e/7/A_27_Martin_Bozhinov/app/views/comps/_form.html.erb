<%= form_with(model: comp, local: true) do |form| %>
  <% if comp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comp.errors.count, "error") %> prohibited this comp from being saved:</h2>

      <ul>
      <% comp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
