<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :task_name %>
  </div>

  <div class="field">
    <%= form.label :decription %>
    <%= form.text_field :decription, id: :task_decription %>
  </div>

  <div class="field">
    <%= form.label :solution %>
    <%= form.check_box :solution, id: :task_solution %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
