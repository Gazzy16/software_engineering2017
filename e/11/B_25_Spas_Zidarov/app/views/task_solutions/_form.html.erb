<%= form_with(model: task_solution, local: true) do |form| %>
  <% if task_solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_solution.errors.count, "error") %> prohibited this task_solution from being saved:</h2>

      <ul>
      <% task_solution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
